name: GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Refresh static API JSON
        run: |
          PYTHONPATH=src python3 - <<'PY'
          import json
          from pathlib import Path
          from florida_seed.meds import FORMULARY
          out = Path("site/api/v1")
          out.mkdir(parents=True, exist_ok=True)
          (out / "drugs.json").write_text(json.dumps({
            "resource": "DrugCatalog",
            "synthetic": True,
            "notice": "Invented generics. Not real INNs or marketed products. Not PHI.",
            "count": len(FORMULARY),
            "items": FORMULARY,
          }, indent=2) + "\n")
          PY
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deploy
        uses: actions/deploy-pages@v4
